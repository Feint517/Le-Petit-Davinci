import 'package:flutter/material.dart';

class PhrasesPracticeScreen extends StatelessWidget {
  const PhrasesPracticeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}